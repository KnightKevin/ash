server:
  port: 3000

spring:
  application:
    name: ash-third-service

  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml

minio:
  endpoint: localhost
  port: 9000
  secure: false
  accessKey: PhPYRy099OodNNcH
  secretKey: MOIZS3OvajiIdV87HONywsbGfOlzCPSl
  bucket: ash